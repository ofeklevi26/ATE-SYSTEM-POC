<Window x:Class="Ate.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ATE UI" Height="360" Width="700">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Device Type"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Text="{Binding DeviceType, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Operation"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Text="{Binding Operation, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Parameters (JSON)"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Text="{Binding ParametersJson, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Vertical">
            <WrapPanel Margin="0,0,0,8">
                <Button Width="90" Margin="0,0,8,0" Content="Send" Click="OnSendClick"/>
                <Button Width="90" Margin="0,0,8,0" Content="Pause" Click="OnPauseClick"/>
                <Button Width="90" Margin="0,0,8,0" Content="Resume" Click="OnResumeClick"/>
                <Button Width="90" Margin="0,0,8,0" Content="Clear" Click="OnClearClick"/>
                <Button Width="120" Content="Abort Current" Click="OnAbortClick"/>
            </WrapPanel>
            <TextBlock Text="Status" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox Text="{Binding StatusText}" IsReadOnly="True" AcceptsReturn="True" Height="120" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
